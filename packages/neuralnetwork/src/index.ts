import { createInitialBrains, createVariation, processLayers } from './core/vanilla'
import type { Brain } from './types'

export type { Brain }
export { createInitialBrains, createVariation, processLayers }
